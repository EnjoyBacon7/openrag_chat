# Backend environment variables

# Database file path (default: chatui.db)
# DB_PATH=chatui.db

# Server port (default: 8080)
# PORT=8080

# Static files directory for production (default: ../frontend/dist)
# STATIC_DIR=../frontend/dist
